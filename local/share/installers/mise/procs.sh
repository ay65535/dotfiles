#!/usr/bin/env bash

# mise plugins ls-remote | grep procs

# apt list procs

# mise ls-remote procs

# sudo apt -y install procs
# apt list --installed procs

# deps
# sudo apt -y install
# mise install procs
# mise use procs

cargo install procs

which -a procs
procs --version

procs
