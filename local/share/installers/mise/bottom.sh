#!/usr/bin/env bash

mise plugins ls-remote | grep bottom

# apt list bottom

# 0.9.6
mise ls-remote bottom

# sudo apt -y install bottom
# apt list --installed bottom

# deps
# sudo apt -y install
mise install bottom
mise use --global bottom

# cargo install bottom

which -a btm
btm --version

btm
